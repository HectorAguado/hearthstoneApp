import { StyleSheet } from 'react-native'

export default StyleSheet.create({ 
    cellContainer: {
        height: 50, 
        borderWidth:1, 
        borderColor: 'blue', 
        alignItems:'center', 
        justifyContent:'center'
    }
})